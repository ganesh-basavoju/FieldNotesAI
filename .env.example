# =============================================
# FieldCapture Pro - Environment Variables
# =============================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to git - it contains secrets!
# =============================================

# --- MongoDB Atlas ---
# Get from: https://cloud.mongodb.com > Database > Connect
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?appName=<appName>
MONGODB_URI=

# --- JWT Authentication ---
# Any strong random string (32+ chars recommended)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# --- AWS S3 Storage ---
# Get from: AWS Console > IAM > Users > Security Credentials
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_BUCKET_NAME=fieldnotes-bucket

# --- N8N Webhook ---
# Your n8n webhook URL for AI processing
N8N_WEBHOOK_URL=

# --- Session Secret (Express) ---
SESSION_SECRET=

# --- Server Port (optional, defaults to 5000) ---
# PORT=5000
