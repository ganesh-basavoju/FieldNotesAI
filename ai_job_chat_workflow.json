{
    "name": "FieldNotes AI Job Chat",
    "nodes": [
        {
            "parameters": {
                "httpMethod": "POST",
                "path": "ai-job-chat",
                "responseMode": "lastNode",
                "options": {
                    "rawBody": false
                }
            },
            "id": "b853e600-4d52-401e-ae47-a758944615d8",
            "name": "Webhook",
            "type": "n8n-nodes-base.webhook",
            "typeVersion": 2,
            "position": [
                224,
                -64
            ],
            "webhookId": "3d5d098f-6b4a-4233-8c90-86e69395d5f5"
        },
        {
            "parameters": {
                "jsCode": "const body = $input.first().json.body;\nconst query = body.query;\nconst context = body.context || [];\nconst history = body.conversationHistory || [];\nconst jobCtx = context[0] || {};\n\nlet contextText = `## Job: ${jobCtx.projectName || 'Unknown'} (ID: ${jobCtx.jobId || 'N/A'})\\n`;\ncontextText += `Mode: ${jobCtx.mode || 'N/A'} | Created: ${jobCtx.createdAt || 'N/A'}\\n`;\n\nif (jobCtx.approvalStatus) {\n  contextText += `Approval: ${jobCtx.approvalStatus}`;\n  if (jobCtx.approvedBy) contextText += ` by ${jobCtx.approvedBy}`;\n  contextText += '\\n';\n}\n\nif (jobCtx.summary) {\n  contextText += `\\n### Summary\\n${jobCtx.summary}\\n`;\n}\n\nif (jobCtx.tasks && jobCtx.tasks.length > 0) {\n  contextText += `\\n### Tasks (${jobCtx.tasks.length})\\n`;\n  jobCtx.tasks.forEach(t => {\n    contextText += `- [${t.status}] ${t.title} (${t.priority}) - ${t.description}\\n`;\n  });\n}\n\nif (jobCtx.issues && jobCtx.issues.length > 0) {\n  contextText += `\\n### Issues (${jobCtx.issues.length})\\n`;\n  jobCtx.issues.forEach(i => {\n    contextText += `- ${i.title}: ${i.description}${i.severity ? ' [' + i.severity + ']' : ''}\\n`;\n  });\n}\n\nif (jobCtx.changeOrders && jobCtx.changeOrders.length > 0) {\n  contextText += `\\n### Change Orders (${jobCtx.changeOrders.length})\\n`;\n  jobCtx.changeOrders.forEach(c => {\n    contextText += `- ${c.title}: ${c.description}\\n`;\n  });\n}\n\nif (jobCtx.transcriptExcerpts && jobCtx.transcriptExcerpts.length > 0) {\n  contextText += `\\n### Transcript Excerpts (${Math.min(jobCtx.transcriptExcerpts.length, 30)})\\n`;\n  jobCtx.transcriptExcerpts.slice(0, 30).forEach(t => {\n    contextText += `${t.speaker ? '[' + t.speaker + '] ' : ''}${t.text}\\n`;\n  });\n}\n\nlet historyText = '';\nif (history.length > 0) {\n  historyText = '\\n## Previous Conversation\\n';\n  history.forEach(h => {\n    historyText += `${h.role === 'user' ? 'User' : 'Assistant'}: ${h.content}\\n`;\n  });\n}\n\nconst fullContext = contextText + historyText;\n\nreturn { context: fullContext, question: query, jobContext: jobCtx };"
            },
            "id": "c8b6afef-8c2e-462d-9106-50a89875f43b",
            "name": "Job Context Formatter",
            "type": "n8n-nodes-base.code",
            "typeVersion": 2,
            "position": [
                464,
                -64
            ]
        },
        {
            "parameters": {
                "modelId": {
                    "__rl": true,
                    "value": "models/gemini-2.5-flash",
                    "mode": "list",
                    "cachedResultName": "models/gemini-2.5-flash"
                },
                "messages": {
                    "values": [
                        {
                            "content": "=You are an AI assistant for a construction field notes application. You are in JOB-LEVEL mode — answering questions about a SINGLE specific job.\n\nRules:\n1. ONLY answer based on the provided context data. Never guess or fabricate.\n2. If the information is not in the context, say so clearly.\n3. Provide source references when possible.\n4. Be concise but thorough.\n5. Format your response clearly with bullet points where appropriate.\n\nCONTEXT (Job Data):\n{{ $('Job Context Formatter').item.json.context }}\n\nUSER QUESTION:\n{{ $('Job Context Formatter').item.json.question }}\n"
                        }
                    ]
                },
                "builtInTools": {},
                "options": {}
            },
            "id": "40ff7986-f7dc-484b-9c63-86a380187d7d",
            "name": "Gemini Job AI",
            "type": "@n8n/n8n-nodes-langchain.googleGemini",
            "typeVersion": 1.1,
            "position": [
                640,
                -64
            ],
            "credentials": {
                "googlePalmApi": {
                    "id": "Icz3qoaROWeSqzkS",
                    "name": "Google Gemini(PaLM) Api account"
                }
            }
        },
        {
            "parameters": {
                "jsCode": "// Extract AI response from Gemini LangChain node\n// The node returns: {\"parts\":[{\"text\":\"...\"}],\"role\":\"model\"}\nconst input = $input.first().json;\n\nfunction extractText(obj) {\n  if (!obj) return '';\n  if (typeof obj === 'string') return obj;\n  // Gemini format: {parts:[{text:'...'}], role:'model'}\n  if (obj.parts && Array.isArray(obj.parts)) {\n    return obj.parts.map(p => p.text || '').join('');\n  }\n  if (obj.text) return typeof obj.text === 'string' ? obj.text : extractText(obj.text);\n  if (obj.output) return typeof obj.output === 'string' ? obj.output : extractText(obj.output);\n  if (obj.response) return typeof obj.response === 'string' ? obj.response : extractText(obj.response);\n  if (obj.content) return typeof obj.content === 'string' ? obj.content : extractText(obj.content);\n  if (obj.message && obj.message.content) return obj.message.content;\n  if (obj.candidates) {\n    try { return obj.candidates[0].content.parts[0].text; } catch(e) {}\n  }\n  return JSON.stringify(obj);\n}\n\nconst aiResponse = extractText(input) || 'Sorry, I could not generate a response.';\n\nconst prevData = $('Job Context Formatter').first()?.json || {};\nconst jobCtx = prevData.jobContext || {};\nconst sources = [];\nif (jobCtx.projectName) {\n  sources.push({\n    projectName: jobCtx.projectName,\n    jobId: jobCtx.jobId || '',\n    type: 'summary',\n    excerpt: (jobCtx.summary || '').substring(0, 100),\n    date: jobCtx.createdAt || ''\n  });\n}\n\nreturn { answer: aiResponse, sources, mode: 'job', processedAt: new Date().toISOString() };"
            },
            "id": "4ad01f55-7e19-4015-aebc-bc4f2ffbb2a7",
            "name": "Response Formatter",
            "type": "n8n-nodes-base.code",
            "typeVersion": 2,
            "position": [
                944,
                -64
            ]
        }
    ],
    "connections": {
        "Webhook": {
            "main": [
                [
                    {
                        "node": "Job Context Formatter",
                        "type": "main",
                        "index": 0
                    }
                ]
            ]
        },
        "Job Context Formatter": {
            "main": [
                [
                    {
                        "node": "Gemini Job AI",
                        "type": "main",
                        "index": 0
                    }
                ]
            ]
        },
        "Gemini Job AI": {
            "main": [
                [
                    {
                        "node": "Response Formatter",
                        "type": "main",
                        "index": 0
                    }
                ]
            ]
        }
    },
    "pinData": {},
    "meta": {
        "instanceId": "8ac1381177916585b7d6f59c47b664e3a554c0eb6b55144045d454d30a938273"
    }
}